<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.mapper.MemberMapper">

	<select id="login" parameterType="HashMap" resultType="MemberDto">
		select *
		from member
		where userid = #{userid} and userpassword = #{userpassword}
	</select>
	
	<insert id="insertMember" parameterType="MemberDto">
		insert into member (userid, userpassword, useremail, useraddress, userinfo)
		values (#{userid}, #{userpassword}, #{useremail}, #{useraddress}, #{userinfo})
	</insert>

	<update id="updateMember" parameterType="MemberDto">
		update member
		set userpassword = #{userpassword}, useremail = #{useremail}, useraddress = #{useraddress}
		where userid = #{userid}
	</update>

	<delete id="deleteMember" parameterType="MemberDto">
		delete from member
		where userid = #{userid} and userpassword = #{userpassword}
	</delete>
	
	<select id="memberList" resultType="MemberDto">
		select *
		from member
	</select>
	 
		<select id="detail" parameterType="String" resultType="MemberDto">
		select *
		from member 
		where userid = #{userid}
	</select>
</mapper>